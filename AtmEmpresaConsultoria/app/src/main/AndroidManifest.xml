<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atmempresaconsultoria.cursoandroid.com.atmempresaconsultoria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/empresa_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EmpresaActivity"
            android:label="@string/title_activity_empresa" />
        <activity
            android:name=".ServicoActivity"
            android:label="@string/servico_activity" />
        <activity
            android:name=".ClienteActivity"
            android:label="@string/clientes_activity" />
        <activity
            android:name=".ContatoActivity"
            android:label="@string/contato_activity"></activity>
    </application>

</manifest>